#include<bits/stdc++.h>
using namespace std;

int main(){
    //It is unordered and unique elements
    //Same as set but not in sorted order
    //insert -> O(1) on average
    //erase -> O(1) on average
    //find -> O(1) on average
    //size -> O(1)
    //empty -> O(1)
    //Lower_bound and Upper_bound functions are not present in unordered_set
    unordered_set<int> s;
    
}